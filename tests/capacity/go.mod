// A capacity harness for an installed JotHost panel. Standard library only,
// like the agent: it is a measuring tool, not part of the product, and it
// should build and run anywhere Go does without pulling anything in.
module github.com/jothost/panel/tests/capacity

go 1.26
