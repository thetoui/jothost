# Scripts

Operational and developer scripts.

Phase 0 drives everything through the `Makefile`, so this directory is a
placeholder. The production installer (`install`, `update`, `repair`,
`uninstall`) lands here in Phase 23.
