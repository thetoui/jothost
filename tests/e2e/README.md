# End-to-End Tests

Browser-level tests covering complete administrator workflows.

Phase 0 has no user-facing workflow to exercise beyond the shell, so this
directory is a placeholder. The first e2e test — login — arrives with Phase 1.
Integration tests live in `tests/integration/`.
